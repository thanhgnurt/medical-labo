import * as userPapeTypes from './../constantActions/userPape';

export const mobileMenuToggle = ()=>({
    type : userPapeTypes.MOBILE_MENU_TOGGLE
})

export const changeTheme = ()=>({
    type : userPapeTypes.CHANGE_THEME,
    
})