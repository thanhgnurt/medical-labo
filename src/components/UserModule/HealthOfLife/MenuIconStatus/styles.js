const styles = theme =>({
    icons :{
        color : theme.palette.primary.textColor,
        opacity : "50%"
    },
    comment :{
        marginBottom : 10,
        marginTop : 5,
    }
});
export default styles;